interface:
  display_name: "KS Codex"
  short_description: "Safely inspect, build, repair, and verify KS projects"
  default_prompt: "Use $ks-codex to inspect, build, repair, or verify this Knowledge Space task through the official API; choose Direct, Coordinated, or Discovery as needed, preserve project scope, and state every browser or live-verification boundary."
policy:
  allow_implicit_invocation: true
